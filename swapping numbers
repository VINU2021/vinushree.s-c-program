#include<stdio.h>
int main()
{
int a,b;
printf("Enter the a and b value :");
scanf("%d%d",&a,&b);
printf("-----Before swapping -----")
printf("a=%d\nb=%d",a,b);
printf("-----After swapping -----")
printf("a=%d\nb=%d",b,a);
return 0;
}
